export default class Weather {
  constructor(day, icon, label, temp, rainProb) {
    this.day = day;
    this.icon = icon;
    this.label = label;
    this.temp = temp;
    this.rainProb = rainProb;
  }
}